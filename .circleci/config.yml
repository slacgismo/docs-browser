version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:8.12
   
    working_directory: ~/

    steps:
      - checkout

      # install dependencies
      #- run: yarn install

      # run tests
      #- run: yarn test

      - run: echo 'Yay we building'